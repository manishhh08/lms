version: 0.2

phases:
  pre_build:
    commands:
      - cd lms-client
      - npm i -g yarn
      - yarn
  build:
    commands:
      - yarn build

artifacts:
  files:
    - "**/*"
  discard-paths: no
  base-directory: lms-client/dist
